vitis_stream_gemm:
  base_cfg: vitis_test
  args:
    stop_at_checkpoint: pd
    benchmark: gemm
    arch_opt_pipeline: streamhls
    pytorch: True

vitis_stream_gesummv_test:
  base_cfg: vitis_test
  args:
    stop_at_checkpoint: pd
    benchmark: gesummv
    arch_opt_pipeline: streamhls
    pytorch: True
    min_dsp: 18

vitis_stream_k2mm:
  base_cfg: vitis_test
  args:
    stop_at_checkpoint: pd
    benchmark: k2mm
    arch_opt_pipeline: streamhls
    pytorch: True
    min_dsp: 18
  
vitis_stream_k3mm:
  base_cfg: vitis_test
  args:
    stop_at_checkpoint: pd
    benchmark: k3mm
    arch_opt_pipeline: streamhls
    pytorch: True
    min_dsp: 15

vitis_stream_bicg:
  base_cfg: vitis_test
  args:
    stop_at_checkpoint: pd
    benchmark: bicg
    arch_opt_pipeline: streamhls
    pytorch: True
    min_dsp: 10

vitis_stream_mvt_1:
  base_cfg: vitis_test
  args:
    stop_at_checkpoint: pd
    benchmark: mvt
    arch_opt_pipeline: streamhls
    pytorch: True
    min_dsp: 10

vitis_stream_atax:
  base_cfg: vitis_test
  args:
    stop_at_checkpoint: pd
    benchmark: atax
    arch_opt_pipeline: streamhls
    pytorch: True
    arch_opt_pipeline: streamhls
    min_dsp: 10

# vitis_stream_sgd_sw:
#   base_cfg: vitis_test
#   args:
#     stop_at_checkpoint: pd
#     benchmark: sgd_sw
#     min_dsp: 100
#     arch_opt_pipeline: streamhls
#     pytorch: True

vitis_stream_syr2k:
  base_cfg: vitis_test
  args:
    stop_at_checkpoint: pd
    benchmark: syr2k
    min_dsp: 50 # not an optimized choice
    arch_opt_pipeline: streamhls
    pytorch: True
    