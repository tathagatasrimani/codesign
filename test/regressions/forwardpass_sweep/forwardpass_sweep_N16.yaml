benchmark_3mm_sweep_test_N16:
  base_cfg: vitis_test
  args:
    benchmark: kernel_3mm
    stop_at_checkpoint: pd
    tech_node: yarch-default
    workload_size: 16
    checkpoint_load_dir: forwardpass_sweep_N16/3mm/
    checkpoint_start_step: schedule

benchmark_2mm_sweep_test_N16:
  base_cfg: vitis_test
  args:
    benchmark: kernel_2mm
    stop_at_checkpoint: pd
    tech_node: yarch-default
    workload_size: 16
    checkpoint_load_dir: forwardpass_sweep_N16/2mm/
    checkpoint_start_step: schedule

benchmark_gemm_sweep_test_N16:
  base_cfg: vitis_test
  args:
    benchmark: gemm
    stop_at_checkpoint: pd
    tech_node: yarch-default
    workload_size: 16
    checkpoint_load_dir: forwardpass_sweep_N16/gemm/
    checkpoint_start_step: schedule

# benchmark_doitgen_sweep_test_N16:
#   base_cfg: vitis_test
#   args:
#     benchmark: kernel_doitgen
#     stop_at_checkpoint: pd
#     tech_node: yarch-default
#     workload_size: 16
#     checkpoint_load_dir: forwardpass_sweep_N16/doitgen/
#     checkpoint_start_step: schedule

benchmark_bicg_sweep_test_N16:
  base_cfg: vitis_test
  args:
    benchmark: kernel_bicg
    stop_at_checkpoint: pd
    tech_node: yarch-default
    workload_size: 16
    checkpoint_load_dir: forwardpass_sweep_N16/bicg/
    checkpoint_start_step: schedule

benchmark_mvt_sweep_test_N16:
  base_cfg: vitis_test
  args:
    benchmark: kernel_mvt
    stop_at_checkpoint: pd
    tech_node: yarch-default
    workload_size: 16
    checkpoint_load_dir: forwardpass_sweep_N16/mvt/
    checkpoint_start_step: schedule

## TODO: Add gemver

## TODO: Add gesummv

benchmark_atax_sweep_test_N16:
  base_cfg: vitis_test
  args:
    benchmark: kernel_atax
    stop_at_checkpoint: pd
    tech_node: yarch-default
    workload_size: 16
    checkpoint_load_dir: forwardpass_sweep_N16/atax/
    checkpoint_start_step: schedule

# TODO: Add trmm
