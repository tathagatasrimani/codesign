benchmark_3mm_test_auto_no_wires:
  base_cfg: vitis_test
  args:
    benchmark: kernel_3mm
    checkpoint_load_dir: tmp_kernel_3mm
    checkpoint_start_step: setup
    stop_at_checkpoint: pd
    max_dsp: 100
    zero_wirelength_costs: true
    ignore_wirelength_costs: true